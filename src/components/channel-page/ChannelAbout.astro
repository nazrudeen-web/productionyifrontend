---
const { data } = Astro.props;

import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Info } from "lucide-react";
import {
  getReadableTopicsAll,
  publishedDate,
  toTitleCase,
  formatNumber,
} from "../../utils/helper.js";

const countryName = new Intl.DisplayNames(["en"], { type: "region" }).of(
  data.country
);
---

<Card>
  <CardHeader>
    <CardTitle className="flex items-center gap-2 text-lg md:text-xl">
      <Info className="h-5 w-5" />
      About {toTitleCase(data.title)}
    </CardTitle>
  </CardHeader>
  <CardContent className="space-y-4">
    <p class="text-slate-700 leading-relaxed">
      <strong>{toTitleCase(data.title)}</strong> is a YouTube channel focused on
      <strong> {getReadableTopicsAll(data.topicCategories)}</strong>. It was
      created on{" "}
      <strong>{publishedDate(data.publishedAt)}</strong> and has attracted{" "}
      <strong>{formatNumber(data.subscriberCount)} subscribers</strong>,
      <strong> {formatNumber(data.viewCount)} views</strong>, and published{" "}
      <strong>{data.videoCount} videos</strong> so far.
    </p>

    <p class="text-slate-700 leading-relaxed">
      Based in <strong>{countryName}</strong>, this channel shares content that
      reaches viewers across different countries. Whether large or small, its
      steady growth shows the impact of consistent uploads and engaging topics
      on YouTube.
    </p>

    <p class="text-slate-700 leading-relaxed">
      Many viewers search for details about{" "}
      <strong>{toTitleCase(data.title)} earnings, income, and net worth</strong
      >, showing strong interest in the creator’s journey and success on
      YouTube.
    </p>

    <div
      class="mt-6 grid grid-cols-2 gap-4 rounded-lg bg-slate-50 p-4 md:grid-cols-4"
    >
      <div class="text-center">
        <p class="text-2xl font-bold text-slate-900">
          {formatNumber(data.subscriberCount)}
        </p>
        <p class="text-sm text-slate-600">Subscribers</p>
      </div>
      <div class="text-center">
        <p class="text-2xl font-bold text-slate-900">
          {formatNumber(data.viewCount)}
        </p>
        <p class="text-sm text-slate-600">Total Views</p>
      </div>
      <div class="text-center">
        <p class="text-2xl font-bold text-slate-900">{data.videoCount}</p>
        <p class="text-sm text-slate-600">Videos</p>
      </div>
      <div class="text-center">
        <p class="text-2xl font-bold text-slate-900">
          {new Date().getFullYear() - new Date(data.publishedAt).getFullYear()}
        </p>
        <p class="text-sm text-slate-600">Years Active</p>
      </div>
    </div>
  </CardContent>
</Card>
